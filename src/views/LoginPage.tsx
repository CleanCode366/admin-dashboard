import LoginCard from '@/LoginCard'
import React from 'react'

const LoginPage = () => {
    return (
        <React.Fragment>
            <LoginCard />
        </React.Fragment>
    )
}

export default LoginPage